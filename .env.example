# CET46 环境变量示例
# 复制本文件为 .env 并按需修改。.env 不会被提交到版本库。

# === Electron 桌面端 ===
# 便携版数据目录（优先级最高，设置后覆盖默认 userData 路径）
# CET46_USER_DATA_DIR=/path/to/data

# 由 electron-builder 便携包自动注入，无需手动设置
# PORTABLE_EXECUTABLE_DIR=

# === 构建模式 ===
# Vite 构建模式（通过 npm_lifecycle_event 自动判定，一般无需手动设置）
# build:file    -> 生成 Electron/Capacitor 使用的 file:// 协议包
# vercel-build  -> 生成 Vercel 部署用的相对路径包
# NODE_ENV=production

# === CI / 测试 ===
# CI 环境标记（Playwright 用：CI=1 时启用重试 + 单 worker）
# CI=1

# === Git 提交身份（仅 scripts/check-with-logging.mjs 使用）===
# GITHUB_ACTOR=your-github-username
